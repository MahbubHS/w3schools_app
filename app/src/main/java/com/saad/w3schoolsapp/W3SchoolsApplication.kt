package com.saad.w3schoolsapp

import android.app.Application

/**
 * Intentionally minimal. Kept as a named Application class (rather than the
 * default) so app-wide setup - none needed today - has an obvious home if
 * this ever grows past a single Activity.
 */
class W3SchoolsApplication : Application()
